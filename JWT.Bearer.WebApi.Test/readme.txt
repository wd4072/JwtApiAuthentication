#####前端如何使用########

说明：但api开启验证后，客户端请求被保护的资源时，如果未通过校验后端会向header中写入www-authenticate=Bearer，来告诉客户端需要用Baarer来验证。

如何取得服务器端信任？也就是获取token的过程
首先提供账号密码，后端服务通过校验后，按照一定的规则生成token返回给客户端，客户端拿到token后，把token加到header中访问需要验权接口时，服务器端校验通过后
处理客户请求后返回给客户端。